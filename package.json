{
  "name": "Node-server",
  "version": "1.0.0",
  "description": "A basic server routing",
  "main": "server.js.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "mocha": "^8.4.0",
    "chai": "^4.3.4"
  }
}
